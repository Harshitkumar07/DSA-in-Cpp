** Problem :- **



** Code :- **

class Solution {
  public:
    int removeDuplicates(vector<int> &arr) {
        if (arr.empty()){
            return 0;
        }
        int idx = 0;
        for(int i=0;i<arr.size();i++){
            if(arr[i]!=arr[idx]){
                idx++;
                arr[idx] = arr[i];
            }
        }
        return idx+1;
    }
};

