** Leetcode Problem :- **



** Code :- **

class Solution {
public:
    int findMaxK(vector<int>& nums) {
        int maxK = -1;
        for (int i = 0; i < nums.size(); i++) {
            if (nums[i] > 0) {
                for (int j = 0; j < nums.size(); j++) {
                    if (nums[j] == -nums[i]) {
                        maxK = max(maxK, nums[i]);
                        break; 
                    }
                }
            }
        }
        return maxK;
    }
};

