** Leetcode Problem :- **



** Code :- **

class Solution {
public:
    int findLengthOfLCIS(vector<int>& nums) {
        int ans, cnt = 0, temp = INT_MIN;
        for (int i = 0; i < nums.size(); i++) {
            if (nums[i] <= temp) {
                ans = max(ans, cnt);
                cnt = 0;
            }
            temp = nums[i];
            cnt += 1;
        }
        return max(ans, cnt);
    }
};

