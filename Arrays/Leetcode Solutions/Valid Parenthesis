** Leetcode Problem 20 :- **



** Code :- **

class Solution {
public:
    bool isValid(string s) {
        stack<char> copy;
        for (int i = 0; i < s.length(); i++) {
            char ch = s[i];
            if (ch == '(' || ch == '{' || ch == '[') {
                copy.push(ch);
            } else {
                if (!copy.empty()) {
                    char top = copy.top();
                    if ((ch == '}' && top == '{') || (ch == ')' && top == '(') ||
                        (ch == ']' && top == '[')) {
                        copy.pop();
                    } else {
                        return false;
                    }
                } else {
                    return false;
                }
            }
        }
        return copy.empty() ? true : false;
    }
};

