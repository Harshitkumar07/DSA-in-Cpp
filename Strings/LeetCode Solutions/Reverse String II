** Leetcode Problem 567:- **



** Code :- **

class Solution {
public:
    string reverseStr(string s, int k) {
        int len = s.length();
        for (int i = 0; i < len; i = i + k + k) {
            int j = i, x = j + k - 1;
            while (j <= x) {
                if (x > len - 1) {
                    x = len - 1;
                }
                swap(s[j++], s[x--]);
            }
        }
        return s;
    }
};

