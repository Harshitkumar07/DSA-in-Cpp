** Leetcode Problem :- **



** Code :- **

class Solution {
public:
    int balancedStringSplit(string s) {
        int count = 0, cntLeft = 0, cntRight = 0;
        for (int i = 0; i < s.size(); i++) {
            if (s[i] == 'R') {
                cntRight++;
            } else {
                cntLeft++;
            }
            if (cntRight == cntLeft) 
                count++;
        }
        return count;
    }
};

